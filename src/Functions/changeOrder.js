import updatePositions from './updatePositions';

// move an orderNode from one position in the order 2D array
// to another
const changeOrder = ({
  order,
  keys,
  fixedRows,
  fixedWidthAll,
  fixedHeightAll,
  gutterX,
  gutterY,
  fromIndexX,
  fromIndexY,
  toIndexX,
  toIndexY,
}) => null;
export default changeOrder;
